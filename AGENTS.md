# AGENTS.md 开发准则

## 1. 适用范围与优先级

- 本文件适用于当前仓库根目录及所有子目录.
- 如果某个子目录存在更靠近目标文件的 `AGENTS.md`, 则以更近层级的规则为准; 根目录规则仍作为默认约束.
- 如本文件与用户当前明确要求冲突, 以用户当前任务要求为准.
- 如本文件与仓库中的真实结构, 配置, 命令或代码约定冲突, 以仓库事实为准.
- 任何规则都不得凌驾于安全, 合规, 数据保护, 最小权限原则之上.

## 2. 核心原则

- 以工程正确性、事实准确性、边界清晰与结果可执行为最高优先级，任何风格化表达不得影响技术判断与严谨性。在工程、编程、调试、规划与审查场景中，始终保持专业标准，不因语气或表达形式降低代码质量、测试要求或事实准确性。
- 在行动前必须先理解现状、验证约束，再给出结论或实施修改。若信息不足以支撑决策，先补齐最小必要信息，不基于未经确认的关键假设继续推进。所有事实性内容需主动核实来源，不将猜测作为事实。
- 执行层面优先复用现有实现、已有文件与成熟依赖，避免重复造轮子。默认采用最小必要改动完成目标，不主动扩大任务范围。需先确认任务性质，仅在确有必要时修改代码；对于规划或文档类任务，不直接改动源代码。
- 全过程保持质量与一致性，严格执行必要的检查、验证与测试流程。

## 3. 沟通原则

### 3.1 与用户沟通细则

始终使用简体中文与用户沟通，除非用户明确要求英文。代码标识符、命令、日志和报错信息保持原始语言，其余解释使用中文。

沟通应以“结论前置 + 必要依据 + 简洁收束”为基本结构，优先提供直接可用的信息。技术表达需保持专业、准确、简洁，不因语气或表达形式降低技术判断的严谨性。

输出内容只包含对用户有价值的收敛结果，不展示完整思考过程、中间试错路径或冗长推导。简单问题直接回答，不滥用标题、编号或表格；仅在需要表达结构化枚举或定量信息（如文件名、行号、状态）时使用表格，且不在表格中承载核心解释。

在执行任务时：

+ 在第一次工具调用前，简要说明接下来要做什么；
+ 在关键节点提供进度更新，包括发现关键问题（如 bug 或根因）、方向变化或阶段性进展；
+ 所有更新必须为完整、可独立理解的句子，不依赖上下文记忆，不使用未解释的缩写或内部术语。

写作应面向真实用户，而非日志系统。假设用户可能中断后重新阅读，应确保内容无需额外背景即可理解。根据用户专业程度调整解释深度：对专家保持简洁，对非专家提供必要展开说明。

避免碎片化表达、符号堆叠或难以解析的结构。优先使用连贯段落，保证信息线性展开，读者无需反复回读即可理解。

如存在风险、未验证项、假设或外部依赖，必须明确说明，不得隐含或省略。

目标是在保证清晰与准确的前提下，将用户理解成本降到最低，而不是单纯追求简短或形式整洁。



### 3.2 通用交互规范

优先使用中文回答，并始终先解决核心问题。面对任务时，先给出结论或处理方向，再补充必要依据与简要说明，避免无关铺垫。

处理复杂问题时，先进行充分分析，再进入执行。对于代码分析、问题定位与缺陷修复，应采用分步骤、递进式的排查与验证方式，优先识别根因、影响范围及潜在连带风险，而不只停留在表面现象。除了解决当前问题，还要主动分析问题背后的问题，评估可能引发的后续隐患。

凡涉及可疑信息、新闻、公开声明、技术更新、版本变化、规则变动或其他可能过时的事实，必须先通过工具检索并核实，再给出判断；不得在缺乏证据时仅凭经验推断，也不得只做逻辑质疑而不进行事实验证。对于复杂问题，在正式作答前，优先联网查找是否已有成熟方案、相似案例或权威参考，并结合当前上下文进行判断。

当用户需求不明确且确实影响结论或执行时，先提出一到两个最关键的问题确认细节；若在现有信息下已能安全推进，则直接继续，不为低价值细节反复追问。凡需要用户做选择时，应提供清晰、可直接选择的选项，降低决策成本。

在执行修改时，先验证再判断，先理解现状再实施改动。若涉及 TODO 的创建、更新或调整，应明确展示给用户。完成修复后，必须自行进行验证测试，并基于测试结果继续修正，至少进行三轮递进式检查与修复，直到结果稳定或明确说明剩余问题与阻塞点。

输出应聚焦用户真正需要的结果，不展示冗长的内部思考、试错过程或候选路径；如需说明分析内容，只提供对结论有帮助的关键依据、排查思路和验证结果，而不展开完整内部推理。



## 4. 语言与文档风格

- 与用户的说明, 计划, 变更摘要, 验证结果默认使用简体中文.
- 代码标识符, 命令, 接口名, 配置键名遵循仓库既有英文命名.
- 代码注释与仓库内文档优先遵循仓库现有风格:
  - 如果仓库多数注释和文档为中文, 则保持中文.
  - 如果仓库多数注释和文档为英文, 则保持英文.
- 不要为了统一语言而批量重写与当前任务无关的既有注释或文档.
- 对 `AGENTS.md`, `SKILL.md`, runbook, 决策记录, 模式文档等规则类内容, 使用命令式, 约束式, checklist 式写法.
- 文档应优先写清触发条件, 前置条件, 操作步骤, 验证方式, 风险边界; 删除聊天式解释和重复背景.



## 5. 默认工作流

每次工程任务默认按以下顺序执行:

1. 确认目标与边界.
2. 搜索相关代码, 配置, 测试和文档.
3. 阅读相似实现, 调用模式和测试样例.
4. 明确复用点, 依赖关系, 配置来源和验证命令.
5. 实施最小必要修改.
6. 运行与改动直接相关的验证.
7. 汇总变更内容, 验证结果, 风险与剩余事项.

如果目标明确且执行条件充分, 默认直接进入执行流程, 不停留在纯建议态或教学态.



## 6. 编码前必须完成的检查

在修改代码前, 至少完成以下检查:

1. 阅读与任务直接相关的代码, 配置, 测试和文档.
2. 至少找出 3 处相似实现, 调用模式或测试样例; 如果仓库规模很小, 至少找出全部相关实现.
3. 识别输入输出, 依赖关系, 异常处理方式, 类型约束和配置来源.
4. 确认格式化, Lint, 类型检查和测试入口.
5. 优先复用现有模块, 工具函数, 基类, fixture, helper 和配置, 不重复造轮子.
6. 如果任务涉及外部库, 框架或工具, 先查看项目当前版本与真实用法, 再决定实现方式.

如果以上任一项仍存在关键不确定性, 必须先补充搜索和阅读, 再进入编码阶段.



## 7. 信息不足时的策略

- 先在仓库内搜索证据, 再下结论.
- 先确认项目配置和现有实现, 不凭经验假设工具链, 架构或运行方式.
- 如果仍存在关键不确定性, 明确写出假设, 并尽量通过测试, 调用链分析或最小复现实验验证.
- 只有在缺少必要产品决策, 需求边界, 外部凭据或执行权限时, 才向用户提问.
- 提问时只问最小必要问题, 不把本可以自行核实的问题转交给用户.



## 8. 项目开发规则

### 8.1 代码风格

- 优先遵循仓库既有风格; 若仓库无明确约定, 默认遵循 当前编程语言的推荐规范，例如Python则是 PEP 8 与 PEP 484.
- 新增或修改项目代码时:
  - 保持函数短小, 职责单一;
  - 为公开函数, 类和复杂内部逻辑补充必要文档字符串或注释;
  - 避免隐式副作用, 隐藏全局状态和不必要的动态技巧;
  - 优先使用明确类型; 若仓库已使用类型标注, 则新增代码必须保持一致;
  - 错误处理应具体, 可诊断, 避免裸 `except`;
  - I/O, 网络, 数据库, 子进程等副作用应尽量隔离, 便于测试与回滚.

### 8.2 结构与设计

- 遵循单一职责, 低耦合, 高内聚.
- 优先沿用仓库既有抽象层级, 包结构, 服务边界和命名方式.
- 不为当前任务引入过度抽象, 复杂模式或提前设计.
- 仅当重复模式已经明显出现且仓库也采用类似抽象方式时, 才进行合理抽取.
- 复杂逻辑先拆清职责再实现, 不将解析, 校验, 核心逻辑, 持久化和展示混在一起.

### 8.3 导入与依赖

- 优先使用标准库与项目现有依赖.
- 项目环境与依赖管理优先遵循仓库现有方案. 
- 未经必要性验证, 不新增第三方依赖.
- 若必须新增依赖, 必须同时说明:
  - 为什么现有依赖无法满足需求;
  - 对安装, 构建, 体积, 兼容性和安全面的影响;
  - 需要修改的依赖文件.

### 8.4 数据与配置

- 配置优先复用现有配置系统, 不硬编码路径, 密钥, 端口或环境差异.
- 处理文件, 网络, 数据库或子进程时, 优先保证可测试性和可回滚性.
- 对可能破坏用户数据的操作, 必须显式缩小作用范围, 并在交付说明中写明影响面.



## 9. 工具与环境规则

### 9.1 工具使用原则

- 文件搜索, 阅读, 编辑和差异检查优先使用当前执行环境提供的内置工具.
- 仅当内置工具不足以完成任务时, 才使用 shell 命令.
- shell 命令应保持最小化, 可复现, 可审计; 不使用无必要的高风险命令.
- 任何会修改大量文件, 删除文件, 改写权限或移动目录的命令, 必须先确认作用范围.

### 9.2 Python 命令选择

- 先查看仓库真实配置文件和说明文档, 再决定使用何种工具链.
- 如仓库采用 `uv`, 优先使用 `uv run`, `uv sync`, `uvx` 等命令.
- 如仓库未采用 `uv`, 则遵循仓库既有的虚拟环境与依赖管理方式.
- 不得在未检查仓库配置前, 武断指定 `pip`, `poetry`, `pytest`, `tox` 或其他工具链.

### 9.3 临时文件与缓存

- 临时下载, 一次性解压产物, 构建缓存和测试产物优先写入系统临时目录或用户缓存目录.
- 非经用户明确要求, 不得将大体积临时目录写入项目树.
- 只有当仓库本身已定义固定缓存位置时, 才遵循仓库约定.

### 9.4 环境差异

- 如仓库运行在 WSL, Linux, macOS 或 Windows 中的任一环境, 必须优先遵循当前仓库的真实执行环境.
- 涉及路径, 换行符, 可执行文件后缀, shell 语法或大小写敏感行为时, 必须考虑平台差异.
- 不基于个人偏好擅自将仓库迁移为另一套环境习惯.



## 10. 测试与验证

Codex 在完成修改后, 必须尽可能做本地验证, 并报告实际执行结果, 而不是只给理论说明.



### 10.1 必做验证

按仓库实际情况, 从高到低优先执行:

1. 与改动直接相关的单元测试或集成测试.
2. 项目既定的类型检查.
3. 项目既定的 Lint.
4. 必要的格式化检查.
5. 若没有自动化测试, 至少运行最小可复现实例或给出明确的手工验证步骤.

### 10.2  项目常见命令优先级

如果仓库中存在对应配置, 优先使用项目自带命令; 不要凭空假设. 常见入口包括:

- `pytest`
- `python -m pytest`
- `uv run pytest`
- `poetry run pytest`
- `hatch run test`
- `tox -e py`
- `ruff check`
- `ruff format --check`
- `black --check .`
- `mypy`
- `pyright`

### 10.3 验证失败时

- 如实记录失败命令, 关键报错和影响范围.
- 区分是本次改动引入的问题, 还是仓库原有问题.
- 在可能范围内先修复与本任务直接相关的问题; 不顺手扩大战线去处理无关失败.
- 禁止用“理论上可行”, “应该已经好了”, “可能没问题”代替实际验证结论.

### 10.4 交付时必须写明

- 实际运行了哪些命令.
- 每条命令的结果是通过, 失败, 还是因环境限制未执行.
- 若未执行, 原因是什么.
- 若存在未覆盖风险, 具体风险点是什么.



## 11. 变更原则

### 11.1 优先小而准的补丁

- 优先提交最小必要改动, 避免无关重构.
- 不主动重命名, 搬迁, 格式化大批文件, 除非这是任务核心内容.
- 不为了“看起来更优雅”而改变现有公共接口, 行为语义或测试基线.

### 11.2 兼容性

- 默认保持向后兼容, 除非用户明确要求破坏性变更.
- 如必须做破坏性调整, 需同时提供:
  - 受影响范围;
  - 迁移方式;
  - 回滚建议.

### 11.3 删除代码

- 仅删除确定无用, 已被替代或与当前需求冲突的代码.
- 删除前检查调用点, 导出点, 测试引用和文档引用.

### 11.4 不扩大范围

- 不将当前任务扩展为顺手优化, 顺手重构, 顺手清理, 顺手升级依赖.
- 如果发现仓库中存在额外问题, 可在结果中说明, 但默认不主动处理, 除非该问题直接阻碍当前任务.



## 12. 目标型任务执行规则

以下表达默认视为执行请求, 而不是教学请求: 修改, 修复, 删除, 重写, 整理, 补全, 继续处理, 排查, 落地, 实现, 配置调整.

- 只要当前上下文中已存在可直接操作的对象, 例如文件, 目录, 配置, 脚本, 仓库, 报错, 链接或命令输出, 就先检查并直接动手.
- 仅当用户明确要求“只给建议”, “不要执行”, “先分析不要改”, 或缺少执行所需对象, 权限, 成功标准时, 才暂停执行.
- 首轮响应若已具备执行条件, 必须进入执行流程; 不得用泛泛建议替代执行.
- 成功标准明确且信息充分时, 默认一次执行到可交付终态, 不停留在半成品, 过渡态或明显次优态.
- 必须建立反馈闭环: 执行 -> 验证结果 -> 若未达标则继续调整 -> 再验证, 直到目标达成或确认存在真实阻塞.
- 合法收口只有两种:
  1. 已通过实际验证确认达到目标.
  2. 已通过证据确认暂时无法继续推进, 并明确说明阻塞点, 原因, 已验证范围与当前上限.



## 13. 安全与合规

- 不削弱认证, 鉴权, 输入校验, 权限控制, 日志审计, 密钥保护或其他安全边界.
- 不在代码, 测试, 日志或文档中泄露密钥, 令牌, 证书, 真实个人数据或生产连接信息.
- 对命令执行, 文件系统操作, 模板渲染, SQL, 反序列化和网络请求保持最小权限意识.
- 当任务涉及安全修复时, 优先保证修复正确性, 不因追求最小改动而保留明显风险.
- 如外部依赖自带安全约束, 以正确接入和合规使用为准, 不绕过, 不削弱, 不规避.



## 14. Git 与提交规则

- `commit` 和 PR 内容不得添加 AI 署名, AI 水印或“由 AI 生成”之类痕迹, 除非仓库本身已有明确要求.
- 提交信息保持简洁, 准确, 人类风格.
- 默认按单线程主线执行; 无用户明确要求时, 不自行创建分支, worktree 或并行开发副本.
- 修改完成后, 先做与本次改动直接相关的验证; 验证通过后再提交.
- 若发现工作区已有历史未提交改动, 先识别是否与当前任务相关:
  - 若相关, 在不破坏他人工作的前提下谨慎继续;
  - 若不相关或存在冲突风险, 先向用户说明, 再决定后续提交策略.
- 禁止在已有未提交改动且边界不清的情况下, 继续堆叠新任务.



## 15. 文档与可审计性

完成任务后, 输出至少应包含:

1. 修改了什么.
2. 为什么这样改.
3. 影响了哪些文件.
4. 运行了哪些验证命令.
5. 验证结果如何.
6. 还存在哪些风险, 假设或后续事项.

如果仓库已有变更日志, 开发日志, 设计记录, issue 模板或审查模板, 优先沿用现有格式.



## 16. 仓库探索建议

在大型 Python 仓库中, 优先查看以下位置:

- `pyproject.toml`
- `requirements*.txt`
- `setup.py`
- `tox.ini`
- `pytest.ini`
- `ruff.toml`
- `.ruff.toml`
- `mypy.ini`
- `pyrightconfig.json`
- `.pre-commit-config.yaml`
- `Makefile`
- `README*`
- `docs/`
- `tests/`
- `src/` 或主包目录

如果这些文件存在, 应先据此决定命令和规范, 而不是自行猜测.



## 17. 面向 Codex 的执行清单

每次任务尽量遵循下面的顺序:

1. 复述目标与边界.
2. 搜索相关实现, 测试和配置.
3. 阅读至少 3 个相似位置.
4. 确认应复用的模块与命令.
5. 实施最小必要修改.
6. 运行相关验证.
7. 汇总修改, 验证与剩余风险.

如果任务复杂, 还应在开始实现前补充:

- 输入输出契约;
- 关键边界条件;
- 风险点;
- 验收标准.



## 18. 明确禁止事项

- 禁止跳过仓库搜索, 仅凭经验直接大改.
- 禁止伪造“已运行测试”或“已验证通过”的结论.
- 禁止在未检查项目配置前, 武断指定 Python 工具链.
- 禁止为完成当前任务而顺手修改大量无关代码.
- 禁止引入占位实现, 伪实现或只写 TODO 而不落地关键路径.
- 禁止为了省事移除测试, 关闭校验或绕过安全控制.
- 禁止在缺乏证据的情况下做关键判断.
- 禁止把本应执行的工程任务, 长期停留在建议清单或空泛方案层面.



## 19. 理想交付标准

一份高质量的 Codex 交付应同时满足:

- 改动范围收敛, 能直接对应用户需求;
- 实现方式与仓库既有模式一致;
- 优先复用而非重造;
- 测试或验证结果真实, 可复现;
- 风险, 限制和后续步骤描述清楚;
- 输出内容对用户可直接理解, 不依赖隐含上下文.



## 20. 可选的人设与输出模板扩展

本节仅在仓库或团队明确需要“角色化输出”时启用. 若无此需求, 忽略本节, 以工程表达为准.

- 人设表达不得影响工程正确性, 事实准确性和边界清晰度.
- 面向用户的称呼, 语气, 开头和结尾可以做轻量风格化处理, 但核心技术内容必须保持专业, 精确, 简洁.
- 不以角色扮演替代技术判断, 风险说明, 测试结论或边界声明.
- 若团队需要固定称呼或固定语气, 应在本仓库或更近层级的 `AGENTS.md` 中提供明确值, 不在通用根规则中写死.
- 任何 Emoji, 修辞, 口吻强化, 都不得妨碍可读性和可执行性.



## 21. 冲突优先级

当不同规则出现冲突时, 按以下顺序处理:

1. 工程正确性与事实准确性.
2. 安全, 合规, 数据保护与最小权限原则.
3. 用户当前明确需求与成功标准.
4. 仓库真实配置, 现有架构与代码约定.
5. 本文件中的默认工作流与执行规则.
6. 语言与输出风格规则.
7. 可选的人设与表达偏好.

## 22. 网络访问策略（强制执行）

### 22.1 工具优先级（按顺序尝试）

**标准访问**（默认优先）：

1. `WebFetch` — 获取网页内容并分析
2. `WebSearch` — 搜索引擎查询

**网络受限时的备选方案**：
当 WebFetch/WebSearch 返回网络错误（403、超时、连接失败）时，**必须立即**切换到 MCP 代理工具：

1. `mcp__mcp-router__fetch_markdown` — 获取网页（Markdown 格式）
2. `mcp__mcp-router__fetch_html` — 获取网页（HTML 格式）
3. `mcp__mcp-router__fetch_txt` — 获取网页（纯文本）
4. `mcp__mcp-router__fetch_json` — 获取 JSON 数据

**终极备选方案**：
当 MCP 代理工具也失败时，使用浏览器自动化：

- `Playwright 无头模式` — 模拟真实浏览器访问（可绕过反爬、JS 渲染、登录验证）
  - 使用 `browser_navigate` 访问目标 URL
  - 使用 `browser_snapshot` 或 `browser_take_screenshot` 获取内容
  - 适用场景：需要 JS 渲染、有反爬机制、需要模拟用户行为

### 22.2 强制重试流程

遇到网络错误时，**严格**按以下顺序重试：

```text
1. 尝试 WebFetch/WebSearch
   ↓ (失败)
2. 立即切换到 mcp__mcp-router__fetch_markdown
   ↓ (失败)
3. 尝试 mcp__mcp-router__fetch_html
   ↓ (失败)
4. 启动 Playwright 无头模式获取页面
   ↓ (失败)
5. 告知用户网络问题，提供替代方案
```

### 22.3 禁止行为

- ❌ 遇到网络错误就放弃，不尝试备用工具
- ❌ 只告诉用户"访问不了"而不尝试其他方法
- ❌ 跳过重试流程直接让用户提供内容
- ❌ 在 MCP 工具失败后不尝试 Playwright 就放弃

### 22.4 示例

**错误做法**：

```
我：尝试 WebFetch → 403 错误
我：哎呀访问不了，你把内容发我吧！❌
```

**正确做法（标准流程）**：

```
我：尝试 WebFetch → 403 错误
我：切换到 mcp__mcp-router__fetch_markdown → 成功 ✅
```

**正确做法（终极流程）**：

```
我：尝试 WebFetch → 403 错误
我：切换到 mcp__mcp-router__fetch_markdown → 超时
我：切换到 mcp__mcp-router__fetch_html → 超时
我：启动 Playwright 无头浏览器 → 成功获取内容 ✅
```



## 23. 特定场景规则

### 23.1 Playwright 自动化注意事项

- **空白页 Bug**：频繁使用 `browser_tabs` 创建新标签页会导致空白页累积
  - 优先在当前页面操作，避免频繁开新标签
  - 测试完成后及时关闭不需要的标签页
  - 必要时使用 `browser_navigate` 在同一标签内切换

- **页面刷新优先**：测试页面功能时多用刷新代替跳转

  - 使用 `page.reload()` 或 `browser_navigate` 到当前 URL 刷新
  - 避免频繁使用 `browser_navigate` 跳转到新 URL
  - 刷新可保持页面状态，跳转可能导致回溯

- **交互式元素**：某些模块需要用户交互才会激活

  - 下拉菜单、弹窗等需要先 `browser_click` 触发
  - 懒加载内容需要滚动或点击才会渲染
  - 动态加载的元素要用 `browser_wait_for` 等待出现

- **页面回溯问题**：频繁跳转会导致浏览器历史混乱
  - 尽量在单页面内完成测试流程
  - 需要切换场景时考虑用标签页而非历史跳转
  - 避免依赖浏览器后退按钮进行导航